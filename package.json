{
  "name": "teak",
  "version": "1.0.70",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "audit:env": "bun --no-env-file run scripts/env-audit.ts",
    "build": "turbo run build",
    "build:cli": "turbo run build --filter=teak-cli",
    "build:extension": "turbo run build --filter=@teak/extension",
    "build:raycast": "turbo run build --filter=./apps/raycast",
    "check": "ultracite check",
    "check:cloudflare": "bun --no-env-file run scripts/check-cloudflare.ts",
    "clean": "test ! -d .turbo/daemon || turbo daemon clean && rm -rf node_modules/.cache && find . \\( -name node_modules -o -name .git \\) -prune -o -name .turbo -type d -prune -exec rm -rf {} +",
    "dev": "bun --no-env-file run scripts/dev.ts",
    "dev:all": "bun --no-env-file run scripts/dev.ts --all",
    "dev:cli": "bun --no-env-file run scripts/dev.ts cli",
    "dev:convex": "bun --no-env-file run scripts/dev.ts convex",
    "dev:desktop": "bun --no-env-file run scripts/dev.ts desktop",
    "dev:docs": "bun --no-env-file run scripts/dev.ts docs",
    "dev:extension": "bun --no-env-file run scripts/dev.ts extension",
    "dev:files": "bun --no-env-file run scripts/dev.ts files",
    "dev:files:local": "bun --no-env-file run scripts/dev.ts files:local",
    "dev:mobile": "bun --no-env-file run scripts/dev.ts mobile",
    "dev:raycast": "bun --no-env-file run scripts/dev.ts raycast",
    "dev:web": "bun --no-env-file run scripts/dev.ts web",
    "doctor": "bun --no-env-file run scripts/doctor.ts",
    "fix": "ultracite fix",
    "lint": "turbo run lint",
    "pre-commit": "turbo run typecheck lint build --affected",
    "prepare": "simple-git-hooks || exit 0",
    "publish:raycast": "cd apps/raycast && bun run publish",
    "release:prepare": "bun --no-env-file run scripts/release-prepare.ts",
    "setup": "bun --no-env-file run scripts/setup.ts",
    "smoke:web": "bun --no-env-file run scripts/smoke-web-session.ts",
    "start": "turbo run start",
    "sync:cloudflare-dev": "bun --no-env-file run scripts/sync-files-worker-dev-secret.ts",
    "test": "turbo run test && bun --no-env-file test scripts/",
    "typecheck": "turbo run typecheck",
    "verify": "turbo run typecheck lint test --affected",
    "verify:all": "turbo run typecheck lint test"
  },
  "simple-git-hooks": {
    "pre-commit": "bun run pre-commit"
  },
  "overrides": {
    "@codemirror/view": "6.43.8",
    "@types/react": "19.2.18",
    "@types/react-dom": "19.2.4",
    "brace-expansion": "5.0.9",
    "convex": "1.46.0",
    "convex-helpers": "0.1.122",
    "esbuild": "0.28.2",
    "playwright-core": "1.62.1",
    "react": "19.2.8",
    "react-dom": "19.2.8"
  },
  "devDependencies": {
    "@biomejs/biome": "2.5.14",
    "js-yaml": "5.4.2",
    "lru-cache": "11.5.2",
    "simple-git-hooks": "^2.13.1",
    "turbo": "^2.10.9",
    "ultracite": "7.12.0"
  },
  "packageManager": "bun@1.4.2",
  "engines": {
    "node": "24.21.0"
  },
  "trustedDependencies": [
    "@sentry/cli",
    "@swc/core",
    "@teak/extension",
    "electron-winstaller",
    "esbuild",
    "sharp",
    "simple-git-hooks",
    "spawn-sync",
    "workerd"
  ]
}
