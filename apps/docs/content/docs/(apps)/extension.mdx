---
title: Browser Extensions
description: Save anything from the web to Teak with the browser extensions
---

Teak for Safari opens your library in a native Mac window and saves the page you are viewing in one click. The Chrome extension adds richer capture options for images, links, selected text, and supported social media posts.

:::note[Safari vs. Chrome feature set]
The Safari toolbar saves the current page. Its Mac app also lets you browse,
search, and read cards. The context menu, inline social-media save buttons, and
other capture options on this page are Chrome/Chromium-only.
:::

## Install

<Tabs param="browser">
  <Tab title="Chrome / Edge / Brave / Arc">
    <Steps>
      <Step title="Edge users: allow other stores">
        In Edge, open `edge://extensions` and turn on **Allow extensions from other stores**.
      </Step>
      <Step title="Open the Chrome Web Store">
        Open the [Teak extension](https://chromewebstore.google.com/detail/teak/negnmfifahnnagnbnfppmlgfajngdpob) in the Chrome Web Store.
      </Step>
      <Step title="Add the extension">
        Click **Add to Chrome** (or your browser's equivalent), then confirm.
      </Step>
      <Step title="Sign in">
        Click the Teak icon in your browser toolbar to sign in.
      </Step>
    </Steps>
  </Tab>
  <Tab title="Safari">
    <Steps>
      <Step title="Install from the Mac App Store">
        Install [Teak for Safari](https://apps.apple.com/us/app/teak-for-safari/id6770003409?mt=12) from the Mac App Store.
      </Step>
      <Step title="Enable the extension">
        Open the app once, then enable the Teak extension in Safari Settings.
      </Step>
      <Step title="Sign in and save">
        Click the Teak icon in the Safari toolbar and choose **Sign in**. Teak for Safari opens so you can approve **Teak Safari** in your browser. Return to the toolbar to save the current page.
      </Step>
    </Steps>
  </Tab>
</Tabs>

The Chrome extension works in Chrome and any Chromium-based browser (Edge, Brave, Arc, etc.). Teak for Safari is a native macOS Safari extension app.

## Safari Extension

Clicking the Safari toolbar extension signs you in when needed and saves the current page URL to Teak when you are signed in.

### Mac library

After you sign in, opening Teak for Safari shows your library. Search by text, choose multiple card types, or show Favorites. Select a card to read its content, notes, summary, tags, and available file details. The card view can open a source link, copy content, or download a file. Use **Save Link** in the header to add a URL, and **Settings** to manage the Safari extension and your account.

## Auto-Save on Open

When you open either extension popup, Teak automatically saves the current page. The popup shows whether the page was saved, was already in your library, or cannot be saved.

In Chrome and Chromium browsers, choose **Upload file** in the popup to save a supported local file up to 100 MB.

## Chrome and Chromium Features

### Context Menu

Right-click anywhere on a page to access quick-save actions:

| Menu item              | What it saves                                      |
| ---------------------- | -------------------------------------------------- |
| **Save Page to Teak**  | The current URL                                    |
| **Save Text to Teak**  | The selected text (creates a text card)            |
| **Save Asset to Teak** | A safely downloadable image, video, audio, or file |

Context menu saves are available on standard web pages. Teak does not attempt to download local, private-network, `file:`, or `blob:` URLs; use **Upload file** when you own a local file.

### Social Media Inline Save Buttons

On supported sites (Instagram, Pinterest, X, Hacker News, Sidebar, Web Designer News, and HeyDesigner), Teak injects a save button directly into each post or story, so you can save individual items without leaving the feed. Buttons appear automatically once you are signed in, and each saved post is deduplicated.

### Free Tier Limit

When you reach your free plan card limit, the Chrome extension popup shows an upgrade prompt instead of saving. Click **Upgrade to Pro** to open Teak Settings.

## Sign In

In Chrome and Chromium browsers, choose **Sign in** in the extension popup and approve Teak in your browser. In Safari, open **Teak for Safari** from the toolbar and approve the connection there. Your connection stays signed in across restarts.

Manage access from Teak Settings → **Security** → **Connections**: disconnecting an app revokes every installation, while signing out in one place disconnects only that installation. In Chrome, interrupted saves stay pending so you can retry them after reconnecting, or discard them from the popup.

## Troubleshooting

<Accordion>
  <AccordionItem title="The extension popup shows an error">
    - Check that you are signed in at [app.teakvault.com](https://app.teakvault.com).
    - Reload the tab you are trying to save and try again.
  </AccordionItem>
  <AccordionItem title="Chrome inline buttons do not appear">
    - Make sure you are signed in to the extension.
    - Reload the page after it fully loads.
    - Verify the extension is enabled in `chrome://extensions`.
    - Inline buttons appear only on the supported sites listed above.
  </AccordionItem>
  <AccordionItem title="Still stuck?">
    Reach out via [Support](/docs/support).
  </AccordionItem>
</Accordion>
