{
  "extends": ["//"],
  "tags": ["app"],
  "tasks": {
    "build": {
      "env": ["GITHUB_SHA", "SENTRY_RELEASE"],
      "inputs": ["$TURBO_DEFAULT$", ".env", ".env.local"],
      "passThroughEnv": ["SENTRY_AUTH_TOKEN"]
    }
  }
}
