# Use default port of 3000, but allow override
PORT=3000

# Node environment
NODE_ENV=development

# Add any other environment variables your app needs
# DATABASE_URL=postgresql://username:password@localhost:5432/teak_db
# JWT_SECRET=your-super-secret-jwt-key
# API_KEY=your-api-key
