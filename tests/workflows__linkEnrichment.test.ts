// @ts-nocheck
import { describe, expect, test } from "bun:test";
import * as module from "../convex/workflows/linkEnrichment";

describe("workflows/linkEnrichment.ts", () => {
  test("module exports", () => {
    expect(module).toBeTruthy();
  });
});
