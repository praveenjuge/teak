// @ts-nocheck
import { describe, expect, test } from "bun:test";
import * as module from "../convex/linkMetadata/types";

describe("linkMetadata/types.ts", () => {
  test("module exports", () => {
    expect(module).toBeTruthy();
  });
});
