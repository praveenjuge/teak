// @ts-nocheck
import { describe, expect, test } from "bun:test";
import * as module from "../convex/workflows/aiMetadata/types";

describe("workflows/aiMetadata/types.ts", () => {
  test("module exports", () => {
    expect(module).toBeTruthy();
  });
});
