// @ts-nocheck
import { describe, expect, test } from "bun:test";
import * as module from "../convex/workflows/steps/classification";

describe("workflows/steps/classification.ts", () => {
  test("module exports", () => {
    expect(module).toBeTruthy();
  });
});
