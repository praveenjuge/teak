// @ts-nocheck
import { describe, expect, test } from "bun:test";
import * as module from "../convex/workflows/steps/renderables/mutations";

describe("workflows/steps/renderables/mutations.ts", () => {
  test("module exports", () => {
    expect(module).toBeTruthy();
  });
});
