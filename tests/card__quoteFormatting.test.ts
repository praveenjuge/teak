// @ts-nocheck
import { describe, expect, test } from "bun:test";
import * as module from "../convex/card/quoteFormatting";

describe("card/quoteFormatting.ts", () => {
  test("module exports", () => {
    expect(module).toBeTruthy();
  });
});
