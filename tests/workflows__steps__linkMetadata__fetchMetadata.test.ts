// @ts-nocheck
import { describe, expect, test } from "bun:test";
import * as module from "../convex/workflows/steps/linkMetadata/fetchMetadata";

describe("workflows/steps/linkMetadata/fetchMetadata.ts", () => {
  test("module exports", () => {
    expect(module).toBeTruthy();
  });
});
