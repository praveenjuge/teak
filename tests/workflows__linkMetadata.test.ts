// @ts-nocheck
import { describe, expect, test } from "bun:test";
import * as module from "../convex/workflows/linkMetadata";

describe("workflows/linkMetadata.ts", () => {
  test("module exports", () => {
    expect(module).toBeTruthy();
  });
});
