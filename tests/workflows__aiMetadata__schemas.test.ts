// @ts-nocheck
import { describe, expect, test } from "bun:test";
import * as module from "../convex/workflows/aiMetadata/schemas";

describe("workflows/aiMetadata/schemas.ts", () => {
  test("module exports", () => {
    expect(module).toBeTruthy();
  });
});
