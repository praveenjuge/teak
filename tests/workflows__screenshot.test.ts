// @ts-nocheck
import { describe, expect, test } from "bun:test";
import * as module from "../convex/workflows/screenshot";

describe("workflows/screenshot.ts", () => {
  test("module exports", () => {
    expect(module).toBeTruthy();
  });
});
